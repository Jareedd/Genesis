{
  "name": "tesla-lyrics-app",
  "version": "1.0.0",
  "private": true,
  "description": "Live-synced lyrics for Tesla in-car browser via Fleet API + LRCLIB",
  "scripts": {
    "install:all": "npm install --include=dev --prefix server && npm install --include=dev --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "NODE_ENV=production npm start --prefix server",
    "render-build": "npm run install:all && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
